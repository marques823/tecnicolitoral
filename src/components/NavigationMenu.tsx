import { useAuth } from '@/contexts/AuthContext';
import { useNavigate, useLocation } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { 
  Home,
  Ticket,
  Users,
  UserCheck,
  BarChart3,
  Tags,
  Settings,
  Crown,
  Sliders,
  FileText,
  Shield
} from 'lucide-react';
import { cn } from '@/lib/utils';

export default function NavigationMenu() {
  // Este componente foi integrado ao Dashboard.
  // O menu responsivo agora é tratado pelo Header.tsx
  return null;
}